name: client_app
description: A Flutter client application for mobile and web.
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tar: ^1.0.0
  image: ^4.0.16
  shelf: ^1.4.1
  http: ^0.13.5
  dio: ^4.0.6
  provider: ^6.0.5
